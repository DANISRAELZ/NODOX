# Public synthetic test data

The CSV files in this directory are a small, synthetic, non-clinical dataset used only for automated tests and reproducible demonstrations.

They do not represent a real isolate, patient, experiment, curated biological claim, or validated therapeutic target. Real user data must be placed in an organism-specific workspace or another ignored local directory and must not replace these files in version control.
