# Real User-Curated Minimal Validation Fixture

This fixture is a portable copy of the minimal `user_curated` validation
dataset. It is intentionally small and stable so tests can verify structure,
provenance and interpretation limits without depending on the ignored
`data_sessions/` workspace.

It does not represent experimental validation, clinical validation, therapeutic
efficacy, low host risk or automatically verified external evidence.
